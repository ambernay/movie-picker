[build]
    publish = '/client'

    functions = 'server/'

    [[redirects]]
        from = "/*"
        to = "/index.html"
        status = 200

    [[headers]]
        for = "/*"
            [headers.values]
            Access-Control-Allow-Origin = "*"

    [[plugins]]
        package = "@netlify/plugin-functions-install-core"